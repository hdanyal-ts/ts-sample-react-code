import styled from 'styled-components'
import { Cell } from '..'
import { colors } from '@design/colors'

export const ActiveBinCell = styled(Cell)`
  background-color: ${colors.green};
  color: ${colors.white};
`
