const { POSITION_TABS } = require('../../')
const { SIDEBAR } = require('../../layout')

module.exports = {
  pageName: '#previous-handoff',
  SIDEBAR,
  POSITION_TABS,
  HANDOFF_HEADER: '#handoff-header',
  HANDOFF_VIEW_BUTTON: '#handoff-view-button',
  INFORMATION_SECTION: '#information-section',
  EMPTY_HANDOFFS: '#empty-handoff',
  DETAIL_SECTION: '#detail-section',
  SUMMARY_SECTION: '#summary-section'
}
