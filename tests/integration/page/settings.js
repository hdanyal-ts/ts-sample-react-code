const { SIDEBAR } = require('./layout')

module.exports = {
  pageName: '#settings',
  SIDEBAR,
  LOGOUT_BUTTON: '#logout-button',
  NAME_FIELD: '#name-field',
  SUPERVISOR_FIELD: '#supervisor-field',
  DATE_FIELD: '#date-field',
  SHIFT_FIELD: '#shift-field',
  CREW_FIELD: '#crew-field',
  POSITIONS_FIELD: '#positions-field'
}
