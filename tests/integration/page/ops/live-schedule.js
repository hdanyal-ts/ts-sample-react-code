const { TEXT_TABS } = require('../')
const { SIDEBAR } = require('../layout')

module.exports = {
  pageName: '#live-schedule',
  SIDEBAR,
  TEXT_TABS,
  LIVE_SCHEDULE_TABLE: '#live-schedule-table'
}
