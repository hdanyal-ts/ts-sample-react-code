const { POSITION_TABS, TEXT_TABS } = require('../')
const { SIDEBAR } = require('../layout')

module.exports = {
  pageName: '#overview',
  CENTERLINE_TABLE: '#centerline-table',
  DEVIATION_HISTORY_BUTTON: '#deviation-history-button',
  POSITION_TABS,
  SIDEBAR,
  TEXT_TABS
}
